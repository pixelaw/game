apiVersion: v1
kind: Service
metadata:
  name: {{ include "pixelaw.fullname" . }}
  labels:
    {{- include "pixelaw.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pixelaw.selectorLabels" . | nindent 4 }}
